# 编排入口

## 机制

人触发编排入口：记得名字、按阶段走。模型发现词表层：靠描述里的分支词。编排入口互不调用，只拉共用词表。主文件只放指针：是什么，何时跟。命中才读下一份。

## 开火

两份入口要同一套问法。加描述或加索引行之前。主文件发胀。人要查「何时用哪份」。

## 怎么叠判断四问

- 依据：谁该记得这份，谁该自动发现。
- 对立：编排入口互相调用；词表复制进每一份入口。
- 这轮：给人看的路由器只能提示，不能替人开火。
- 保护：弱指针等于方差。先改那一行措辞。

## 不接什么

把多份模型糊进一份主文件。让用户报编号。词表层的口吻写进 tta-tone 开口。
