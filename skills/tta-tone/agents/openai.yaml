interface:
  display_name: "TTA Tone"
  short_description: "Natural, concise, semantically faithful agent writing"
  default_prompt: "Use $tta-tone as the final language layer while preserving facts, scope, uncertainty, and technical literals."
policy:
  allow_implicit_invocation: true
