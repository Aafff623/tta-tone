# Changelog

## 0.0.2 - 2026-08-13

### 新功能

- 默认语气开篇必须有主语：先写我们在什么场合、碰到什么问题，再解开
- 语气改成可扩充名单：默认、热情、官方；新人物档加表，不另开 Skill
- 现成颜文字混排嵌在正文转折处，不再只贴在回复末尾
- 讲授去掉课表腔，改成先把人放进场合再解开眼前这个问题
- lint 增加发布腔、英文套话和课表腔检查

### 文档

- README 对齐默认语气、Wiki 入口和官方语气调用示例

## 0.0.1 - 2026-08-13

### 新功能

- 成稿、讲授、答问三条分支共用身份和用词，骨架写在 `references/modes.md`
- 对话调味按场景路由（`done` / `peek` / `think` / `surprise` / `tea` / `wait` / `ack` / `play`），从现成混排抽取
- `scripts/season.py --scene` 支持 `--exclude`，同一会话不重复同一条组合
- 编程工具对话收尾使用横式 `now` / `before` 表，引文框写碎碎念和对话摘要

### 文档

- README 说明三种任务、对话外壳和调味命令
