# Changelog

## Unreleased

### 文档

- `references/voice/` 本 Skill 只读。写入改由博客仓 `knowledge-output` 在发布时做，步骤不写进本 Skill。

### 新功能

- 成稿改跟金样本：经验稿先核现场格子；顺序是时间线 → 干活例子 → 请人进来摊货。`references/canon.md`
- 成稿外挂词表 `references/voice/`：成稿只读 `lexicon.md`
- 外挂知识库 `references/kb/`：线索命中再抽 1–2 篇短卡，不覆盖开口，不报编号。这是工程短卡，不是成稿词表
- 工程轨八篇：先约后做、可执行停手、主干在代码里、注意力预算、窄缝一轮、阶段停点、编排入口、不知道用哪份 Skill 时交给 `ask-matt`
- 探索轨七篇：三条路、部分自主、磁盘记忆、人写说明书、写验拆开、尖齿与沙箱、可核验才进内环

### 文档

- 答问：不知道用哪份开发 Skill 时先接处境，再调用 `ask-matt`
- `SKILL.md` 步骤 2：讲开发 / agent / 何时停时打开 `kb/INDEX.md`
- `layers.md`：开发流程走知识库，不是新招式轴；预写反驳先复述对方中心论点
- 讲授「打开再解」：打开送进这次判断的那一份；未核复杂度一次一层
- 判断「依据」：只填哪次运行 / 哪份 stdout；没跑写未跑
- README 增加「知识库」小节；来源人名只在 `kb/sources.md`

## 0.0.3 - 2026-08-13

### 新功能

- 身份单独写成 `references/identity.md`：开篇主语、长句原因、不编造、不营销、混排嵌转折
- 轻 / 述 / 拆降成隐藏检测，不再让用户报层号；线索命中再抽收窄、先引后判、预写反驳、决策壳
- 成稿两问审计：还剩哪些套话壳；有没有多出材料里没有的事实
- 写完再交付：默认 Markdown；点名才交接 `tta-html` / `tta-ppt` / `tta-motion` / `tta-visual` / `tta-cover`；专业架构走结构图，不指挥生图模型当成品
- lint 增加升华、管理黑话、官腔、鸡汤和客服腔漏网检查

### 文档

- README 和 Wiki 对齐招式检测与写完再交付

## 0.0.2 - 2026-08-13

### 新功能

- 默认语气开篇必须有主语：先写我们在什么场合、碰到什么问题，再解开
- 语气改成可扩充名单：默认、热情、官方；新人物档加表，不另开 Skill
- 现成颜文字混排嵌在正文转折处，不再只贴在回复末尾
- 讲授去掉课表腔，改成先把人放进场合再解开眼前这个问题
- lint 增加发布腔、英文套话和课表腔检查

### 文档

- README 对齐默认语气、Wiki 入口和官方语气调用示例

## 0.0.1 - 2026-08-13

### 新功能

- 成稿、讲授、答问三条分支共用身份和用词，骨架写在 `references/modes.md`
- 对话调味按场景路由（`done` / `peek` / `think` / `surprise` / `tea` / `wait` / `ack` / `play`），从现成混排抽取
- `scripts/season.py --scene` 支持 `--exclude`，同一会话不重复同一条组合
- 编程工具对话收尾使用横式 `now` / `before` 表，引文框写碎碎念和对话摘要

### 文档

- README 说明三种任务、对话外壳和调味命令
